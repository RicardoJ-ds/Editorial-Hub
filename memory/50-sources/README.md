# 50 · Sources — raw founding inputs (immutable)

The **raw source material** the wiki points to and distills — not curated notes. Treat these as
read-only inputs: the PRDs/specs the Hub was built from, the initial sheet exports, and the design
system. The living, distilled versions live in `10-reference/`, `40-metrics/`, and `20-decisions/`.

## Contents
- **`specs/`** — what we set out to build:
  - `PRD_Final_*`, `Claude_Code_Build_Prompt_*`, `v3PRD_Input_Template_*` (`.docx` + `.txt`) — the founding PRDs/build prompt. **Regenerated by `fetch_docs.py`** (its `OUTPUT_DIR` points here). *gitignored* (binaries).
  - `CAPACITY_PLANNING_V2.md` — the CP v2 schema/spec. Living distilled pointer → [[reference-cp-v2]]. *tracked.*
  - `CP2_COVERAGE_AUDIT.md` — the column-level coverage proof (2026-04-18). *tracked.*
- **`seed-data/`** — the initial CSV exports from project start (Mar 2026): `Editorial_SOW_overview`, `Delivered_vs_Invoiced_v2`, `ET_CP_*`, `Operating_Model`, `Delivery_Schedules`, `Engagement_Requirements`, `Meta_Calendar`, `Model_Assumptions`, `all_sheets_combined.txt`. ⚠️ **Sensitive** (client names + revenue/SOW/capacity) → **gitignored**. The live data now flows sheets → warehouse; these are a historical snapshot only.
- **`design-system/`** — `Graphite-Interal-DS.html` (the Graphite Internal DS reference). *tracked.*

## Git posture (see root `.gitignore`)
- **gitignored:** `seed-data/` (all), `specs/*.docx`, `specs/*.txt`.
- **tracked:** `specs/*.md` (CP-v2 docs), `design-system/*.html`.

## Provenance
Consolidated here on 2026-06-22 from the old top-level `data/` (specs + seed exports) and `.docs/`
(design system) + the repo root (CP-v2 spec + coverage audit). See `../log.md`.
